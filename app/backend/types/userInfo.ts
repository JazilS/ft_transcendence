export type UserInfo = {
  id: boolean;
  name: boolean;
  status: boolean;
  pong: boolean;
};

export const userId: UserInfo = {
  id: true,
  name: false,
  status: true,
  pong: true,
};

export const UserData: UserInfo = {
  id: true,
  name: true,
  status: true,
  pong: true,
};
